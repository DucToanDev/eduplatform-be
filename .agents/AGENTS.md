# Database Schema Rules

Any time a new database table or schema is created, or an existing schema is modified (adding, removing, or changing columns/fields), you MUST immediately update the `erd.dbml` file in the root directory to reflect the changes. This ensures the ERD is always up-to-date with the codebase.
